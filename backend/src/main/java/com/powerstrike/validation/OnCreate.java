package com.powerstrike.validation;

/**
 * Grupo de validación que solo se aplica al crear una entidad.
 * Permite exigir la contraseña en el alta de usuario pero hacerla opcional en la edición
 * (DEF-EXP-03: editar un usuario sin cambiar la contraseña).
 */
public interface OnCreate {
}
